# =============================================================================
# EFU Frontend — PRODUCCIÓN (plantilla para el administrador del servidor)
# =============================================================================
# Copiar a frontend/.env.production antes de `npm run build`.
# Los valores se embeben en el build; cambiar dominio = volver a compilar.
# =============================================================================

VITE_API_BASE_URL=https://api.efu.umsa.bo
VITE_API_URL=https://api.efu.umsa.bo/api/v1
VITE_APP_URL=https://efu.umsa.bo
